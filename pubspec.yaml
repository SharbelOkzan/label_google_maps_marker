name: label_google_maps_marker
description: A package to create simple Google Maps marker with label text.
version: 0.0.3
homepage: "https://github.com/SharbelOkzan/label_google_maps_marker"

environment:
  sdk: ">=2.18.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0